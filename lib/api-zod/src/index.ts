// generated/api: zod runtime schemas (values) + inferred types.
export * from "./generated/api";
// generated/types: TypeScript-only model types. Imported as `type` to avoid
// duplicate-export conflicts where the same identifier exists in both files
// (e.g. `UpdateXyzBody` exists as a zod schema AND a TS type after orval v8.5).
export type * from "./generated/types";
