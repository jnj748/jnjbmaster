모든 작업을 중간에 확인 없이 끝까지 자동으로 완료해줘. 진행 여부를 묻지 말고 완료 후 결과만 보고해줘.

## 작업: RFQ·법정업무 알림톡 dispatch 추가 (2~3번)

### 전제
- 발송 채널: aligo_kakao
- 발송 함수: enqueueDispatch (artifacts/api-server/src/lib/external/adapter.ts)
- 환경변수: ALIGO_API_KEY 등 미설정 시 devSimulate 자동 동작 — 실 발송 안 됨
- DB 변경 없음

### 작업 A: RFQ 생성 시 파트너 알림톡 (rfqs.ts)
파일: artifacts/api-server/src/routes/rfqs.ts

RFQ 생성 후 파트너에게 insertNotification 하는 루프 안에
이메일 발송(sendMail) 다음에 aligo_kakao dispatch 추가.

수신자: 해당 vendorId 소속 파트너의 users.phone
조건: phone 이 null 이 아닌 경우만 발송
payload:
{
  templateCode: "rfq_new_partner",
  senderKey: process.env.ALIGO_SENDER_KEY ?? "",
  senderNumber: process.env.ALIGO_SENDER_NUMBER ?? "",
  message: `[관리의달인] 새 견적 요청이 도착했습니다\n\n#{building_name}에서 #{category} 견적을 요청했습니다.\n요청 내용: #{rfq_title}\n위치: #{location}\n마감: #{deadline}\n예상 크레딧 차감: #{credit_cost}C\n\n지금 파트너 포털에서 확인해 주세요.`,
  receiverName: "",
  buildingId: rfq.buildingId
}

실제 발송 시 #{} 변수는 실제 값으로 치환해서 message에 삽입할 것.
변수 매핑:
- building_name: building.name
- category: rfq.category (rfqCategoryLabel 함수로 한글 변환)
- rfq_title: rfq.title
- location: building.address ?? ""
- deadline: rfq.deadline ?? "미정"
- credit_cost: costPreview?.totalCost ?? 0 (없으면 0)

### 작업 B: 견적 제출 시 소장 알림톡 (quotes.ts)
파일: artifacts/api-server/src/routes/quotes.ts

quote_received insertNotification 다음에 aligo_kakao dispatch 추가.
수신자: 해당 building 소속 manager role 유저의 phone
조건: phone 이 null 이 아닌 경우만 발송
payload:
{
  templateCode: "quote_received_manager",
  message: `[관리의달인] 견적서가 도착했습니다\n\n#{rfq_title}에 #{vendor_name}의 견적이 접수되었습니다.\n견적 금액: #{amount}원\n유효기간: #{valid_until}까지\n\n앱에서 확인 후 채택 여부를 결정해 주세요.`,
  변수 매핑:
  - rfq_title: rfq.title
  - vendor_name: quote.vendorName
  - amount: quote.totalAmount.toLocaleString("ko-KR")
  - valid_until: quote.validUntil ?? "미정"
}

### 작업 C: 견적 채택 시 파트너 알림톡 (quotes.ts)
quote accepted 처리 블록 (prev.status !== "accepted" && updated.status === "accepted") 안에
파트너 phone 조회 후 aligo_kakao dispatch 추가.
payload:
{
  templateCode: "quote_accepted_partner",
  message: `[관리의달인] 견적이 채택되었습니다\n\n#{building_name}의 #{rfq_title} 견적이 채택되었습니다.\n채택 금액: #{amount}원\n\n관리소장과 일정을 조율해 주세요.`
}

### 작업 D: 견적 반려 시 파트너 알림톡 (quotes.ts)
quote_rejected insertNotification 다음에 파트너 phone 조회 후 aligo_kakao dispatch 추가.
payload:
{
  templateCode: "quote_rejected_partner",
  message: `[관리의달인] 견적이 반려되었습니다\n\n#{building_name}의 #{rfq_title} 견적이 반려되었습니다.\n사유: #{reject_reason}`
}

### 작업 E: 법정업무 알림톡 — D-7, D-0, 초과 (inspections.ts)
파일: artifacts/api-server/src/routes/inspections.ts

inspection_alert insertNotification 다음에 aligo_kakao dispatch 추가.
수신자: 해당 building 소속 manager, facility_staff role 유저의 phone
조건:
- phone null 아닌 경우만 발송
- 해당 inspection의 alertActions에 completed 액션이 있으면 발송 안 함 (처리완료 건 제외)
- D-7: daysUntil === 7 일 때만
- D-0: daysUntil === 0 일 때만
- 초과: daysUntil < 0 이고 Math.abs(daysUntil) === 1 일 때만 (초과 첫날 1회만)

payload 분기:
- D-7: templateCode "mandatory_d7", message `[관리의달인] #{task_title} 점검이 다가오고 있어요\n\n#{building_name}님,\n#{due_date}까지 #{task_title} 점검이 있습니다.\n\n업체 예약이 필요하다면 견적을 받아보실 수 있어요.`
- D-0: templateCode "mandatory_dday", message `[관리의달인] 오늘 #{task_title} 점검일이에요\n\n#{building_name}님,\n오늘(#{due_date}) #{task_title} 점검이 예정되어 있습니다.\n\n처리 완료 후 앱에 기록해 두시면 보고서에 자동으로 반영됩니다.`
- 초과: templateCode "mandatory_overdue", message `[관리의달인] #{task_title} 점검 일정을 확인해 주세요\n\n#{building_name}님,\n#{task_title} 점검 예정일(#{due_date})이 지났습니다.\n\n처리하셨다면 앱에 완료 표시만 해주세요.\n아직 미처리라면 빠른 일정 조율을 권해드립니다.`

### 검증
pnpm --filter @workspace/api-server run typecheck
오류 없어야 함