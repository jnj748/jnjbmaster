kind = "web"
previewPath = "/"
title = "관리의달인"
version = "1.0.0"
id = "artifacts/manager-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19752

[services.development]
run = "pnpm --filter @workspace/manager-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/manager-app", "run", "build" ]
publicDir = "artifacts/manager-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19752"
BASE_PATH = "/"
